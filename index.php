
<?php

if (
    $databaseDesignComplete === true && 
    $designDiscussedWithTeacher === true && 
    $relationalModelCreated === true && 
    $reportFinished === true
) {
    // Student can proceed to next assignment
    // Database design phase is complete
}
